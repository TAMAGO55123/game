/**
 * 
 * @param {string} text
 * @param {HTMLDivElement} textarea
 * @param {number} time 
 * @param {TextSizepx} textpx 
 */
function textdot(text,textarea,time,textpx){
    let data2=data.split('');
    let data3=window.innerWidth/textpx;
    
}